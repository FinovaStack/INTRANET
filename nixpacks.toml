[phases.setup]
nixPkgs = ["php85", "php85Extensions.pdo_mysql", "php85Extensions.mysqlnd"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"